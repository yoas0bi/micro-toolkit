syntax = "proto3";

package yoas0bi.micro-toolkit.resource;
option go_package = "github.com/yoas0bi/micro-toolkit/pb/resource";

enum VISIABLE_MODE {
    // 默认Namespace可见
    NAMESPACE = 0;
    // 域内可见
    DOMAIN = 1;
    // 全局可见
    GLOBAL = 2;
}